/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/pagina-web',
  images: {
    unoptimized: true,
  },
  reactStrictMode: true,
}

export default nextConfig
